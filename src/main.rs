#![allow(dead_code)]
#![allow(unused_imports)]
use proconio::{input, marker::*};
use std::collections::*;

fn main() {
    input! {
        n: i32,
    };

    let mut ans: i32 = 0;
    println!("{}", ans);
}
